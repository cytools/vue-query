/**
 * Internal dependencies.
 */
import useQuery from '@/vue/query/useQuery';
import useMutation from '@/vue/useMutation';
import useQueryClient from '@/vue/useQueryClient';
import usePaginateQuery from '@/vue/query/usePaginateQuery';
import { queryDataClone, changeQueryDataCloneMethod } from '@/core/query/Query';

export {
    useQuery,
    useMutation,
    useQueryClient,
    usePaginateQuery,
    queryDataClone,
    changeQueryDataCloneMethod,
};
